# File List

- `prompt-input/`: prompt input 共享组件集合目录。
- `actions.tsx`: 提供可复用的 actions 共享能力。
- `conversation.tsx`: 提供可复用的 conversation 共享能力。
- `filelist.md`: 记录当前目录下一层文件和子目录的大致作用。
- `loader.tsx`: 提供可复用的 loader 共享能力。
- `message.tsx`: 提供可复用的 message 共享能力。
- `model-selector.tsx`: 提供可复用的 model selector 共享能力。
- `prompt-input-contract.test.ts`: 验证 prompt input contract 相关行为的自动化测试。
- `prompt-input.tsx`: 提供可复用的 prompt input 共享能力。
- `reasoning.tsx`: 提供可复用的 reasoning 共享能力。
- `response.tsx`: 提供可复用的 response 共享能力。
- `shimmer.tsx`: 提供可复用的 shimmer 共享能力。
- `sources.tsx`: 提供可复用的 sources 共享能力。
- `suggestion.tsx`: 提供可复用的 suggestion 共享能力。
