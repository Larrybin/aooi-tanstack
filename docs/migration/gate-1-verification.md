# Gate 1 Verification

Recorded on 2026-06-10 from branch `codex/tanstack-start-native-gate-0-3`.

## Commands Run

```bash
pnpm install --no-frozen-lockfile
SITE=dev-local pnpm tanstack:validate
SITE=dev-local pnpm tanstack:typecheck
SITE=dev-local pnpm tanstack:build
```

## Result

- Status: passed
- Lockfile: regenerated by `pnpm install --no-frozen-lockfile`.
- Notes: pnpm reported existing peer warnings for Fumadocs with Next 16 and a Vite peer warning for `fumadocs-mdx`; TanStack install, validation, typecheck, and build completed.
- TanStack SSR uses a Vite SSR-only `server-only` marker resolution to
  `server-only/empty.js`. This preserves the existing static server marker
  semantics for server builds without adding a Next bridge or client alias.
